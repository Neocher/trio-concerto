# 三体重编排修正规格 — P1a 空转误判修复 (git diff 回退判定)

## 0. 元信息
- 目标仓库: /home/user/trio-concerto (git 仓库) | 执行方: herdr muse tab
- 规格位置: 本文件 (仓库内, 直接可读)

## 1.【缺口】
三体流水线 task 68646cbd (2026-09-23, dsh-tool-taiji 插件修复) 实证:
OpenCode 两轮**都真实改盘** (git diff +265/-118, 11 条契约全部落地),
但桥返回 output=0 chars (rc=0) → 编排层 /home/user/trio-concerto.py 行 407
`if not best_code.strip():` 判"空转" → 跳过 Codex 审核 → 连续 2 轮后熔断
报"未通过" — 实际代码是合格的 (后由 Hermes 亲验: tsc 0 error + 37 测试全绿 + Codex 补审通过)。
根因: 空转判定只看 LLM output 通道, 不看文件系统副作用。
"改盘成功但输出为空" ≠ "空转"。

## 2.【新增能力】
修改 /home/user/trio-concerto.py (MCP 编排层):
- **C1 git diff 回退判定**: 在 P1a 空转判定处 (行 ~407 `if not best_code.strip():` 附近),
  当 output 为空时, 额外检查 `workdir` 的 git 状态 (workdir 参数已传入, 见 _dispatch/行 ~158 注释;
  若该处拿不到 workdir 则从 task 上下文/环境变量取, 找不到就保持旧行为):
  - 执行 `git -C <workdir> status --porcelain` (subprocess, 10s 限时, 异常视为无变化)
  - 有非空输出 (工作区有改动) → **不判空转**: 视为"静默交付", 正常进入 Codex 审核,
    并在工件 code-{N}.md 顶部写入一行 `> [编排层注] OpenCode output 为空但 git diff 非空, 按有交付处理`
    供审核腿知道上下文; report 日志标注 "(静默交付: git diff 非空)"
  - git 无变化或 git 不可用 (非 git 仓库) → 维持现状 (计空转 streak, 2 连熔断)
- **C2 判定留痕**: 空转/静默交付判定结果写入 .trio/run/<task_id>/run-summary.md
  (该文件已有, 追加一段 `## 空转判定审计` 列每轮: output 长度 / git 变化数 / 判定)
- 白名单文件: /home/user/trio-concerto.py (只改这一个)
- 禁止: 改 acp_bridge.py; 改判定语义外的逻辑; 引入新依赖; 删除现有 P1a 熔断机制
  (git diff 非空只是"不判空转"的例外分支, 空 diff 时空转熔断照旧)

## 3.【判据】
确定性门:
- `python3 -m py_compile /home/user/trio-concerto.py` exit 0
- 逻辑核验 (读代码确认): output 空 + git 有改动 → 走审核分支; output 空 + git 无改动 → 空转分支; output 非空 → 原路径不变
- 单测: 在仓库内建 /home/user/trio-concerto/tests/test_stall_git.py,
  mock subprocess 的 git 调用 (有改动/无改动/非 git 仓库 三种), 断言三种路径判定正确,
  用 pytest 跑通 (仓库若无 pytest 依赖则用 python3 -m unittest 或独立脚本断言, 贴真实输出)
回归门:
- `git -C /home/user/trio-concerto diff --name-only` 只含白名单文件 (+新增测试文件)
- 现有 /tmp/test_p1a_stall.py 若仍可跑 (5 用例) 必须仍通过 (该文件在 /tmp 可能已清, 不存在则跳过并注明)

## 4.【失效条件】
- git 检查在超大仓库上 >10s → 限时兜底 (超时按"无变化"处理, 回到旧空转语义, 宁误判不卡死)
- 非 git 目录 workdir → git 命令失败按无变化处理 (保持旧行为, 不报错中断流水线)
- 若"静默交付"分支让审核腿拿到空实现说明 → code-{N}.md 顶部已注入编排层注记 + 审核 prompt
  本就要求现场核验 git diff (见 skill), 风险可接受
- 回滚: 单 git 提交, `git revert` 即恢复

## 交付摘要要求 (缺一项即打回)
1. git status 原文
2. py_compile + 单测真实输出
3. 三种路径 (git 有改动/无改动/非 git 仓库) 的判定逻辑代码段 (行号引用)
4. 与规格差异逐条说明
